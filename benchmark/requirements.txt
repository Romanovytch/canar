# Benchmark dependencies. Canonical declaration: the `benchmark` extra in
# ../pyproject.toml (pip install -e "..[benchmark]" pulls these plus canar).
# This plain list stays for a lightweight standalone venv that doesn't install
# the full canar package; keep the two in sync.
ragas>=0.3
langchain-openai>=0.2
python-dotenv>=1.0
pandas>=2.0
qdrant-client>=1.10
pyyaml>=6.0
# Sparse query encoder (BM25) for the simple_sparse and hybrid strategies.
fastembed>=0.8.0
